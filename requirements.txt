# ============================================================
# DRHP Drafting Agent — Python 3.13 / Apple Silicon (arm64)
# Install: pip install -r requirements.txt
# ============================================================

# PDF processing
pdfplumber>=0.11.0          # 0.11.x tested on Python 3.13
pymupdf>=1.24.0             # 1.24+ required for arm64 wheels

# Token counting
tiktoken>=0.12.0

# Embeddings and vector search
sentence-transformers>=3.0.0   # 5.x tested; 2.x API is incompatible
faiss-cpu>=1.7.4               # 1.13.x tested

# Numerical / data
numpy>=1.26.0
pandas>=2.2.0               # 2.2+ required for Python 3.13 wheels
tabulate>=0.9.0             # required by pandas DataFrame.to_markdown()

# LLM API — Azure OpenAI
openai>=2.0.0               # Azure OpenAI uses the openai SDK (2.x API)
